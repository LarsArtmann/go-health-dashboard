# Status Report — v0.10.0 / v0.10.1 Release Session

**Date**: 2026-09-22 ~23:50 CEST · **Session scope**: the v0.10.0 cut ("Time for v0.10.0?"),
plus the unplanned FOD repair it opened with and the green-CI re-cut it ended with.
**Result**: v0.10.1 is Latest (tag `e801bf9`, CI green, proxy+sumdb+clean-consumer verified);
v0.10.0 stays published as a disclosed supersede (identical Go code).

---

## What landed

1. **health-hub FOD repair** (pre-release, blocking): the fleet normalize downgrade of the
   `go` directive (swept by the daemon in `d6b0f7a`) broke every module load against
   go-health v0.4.0's floor → restored atomically (`8815ee7`). The v0.4.0 pin had also
   left `packages.health-hub` with a stale vendorHash (last computed against the 2026-09-18
   pseudo-version) → updated from nix's own FOD output (`5ee8dfc`); buildflow's nix-hash-fix
   was retried twice but is environmentally blocked (see gotchas).
2. **v0.10.0 cut**: full checklist §1-§5 (reconcile, pins, fuzz registry, ruleset, gate 0,
   CHANGELOG re-head, const+stamps, all gates, tag-first push). The tag commit (`ea5ed88`)
   then failed two hygiene CI jobs → **v0.10.1 re-cut** (`e801bf9`, green) with:
   the missing `[0.10.0]` footer link; the treefmt goimports go-floor fix (flake.nix wraps
   goimports with `goPkg`; nixpkgs' wrapper pinned 1.26.x and the sandbox has no network);
   the release-draft awk escape fix (`83962f4`, index() instead of regex).
3. **Release pages**: both created manually (backfill convention) — v0.10.1 Latest,
   v0.100 non-latest with a supersede disclosure.
4. **Close-out**: TODO_LIST rows removed (cut v0.10.0, release timing, release-draft
   exercise), FEATURES Released row carries verify evidence, three new AGENTS.md gotchas.

## Verification evidence

- `verify-release.sh v0.10.1`: checks 1-4 OK (tag/proxy hash/sumdb/clean-consumer/page);
  check 5 FAIL is the known exception (the frozen broken release-draft run on the tag);
  check 6 skipped (HEAD past tag). CI workflow green on `e801bf9` (`gh run watch` exit 0).
- Local gates at the tag: build/test-race/lint/vet/browser/vulncheck/fmt/checks.format/
  health-hub FOD/pins/changelog-lint all green (the treefmt-check fix is provable locally —
  the sandbox has no network here either).

## What I got wrong (honesty section)

- Diagnosed treefmt-check's local failure as the "chronic DNS blocker" gotcha; it was
  deterministic (no-network sandbox + toolchain-switch attempt). CI's identical failure
  forced the correction — the contradiction stop-loss did its job.
- Cut v0.10.0 without re-running the changelog lint AFTER the re-head (gate 0 ran before
  the edit); the missing footer link cost the tag its green CI.
- Ran repo scripts outside the devShell twice (ambient go 1.26.7 trap) and used a generic
  `/tmp/g2.log` that a parallel session clobbered, producing a phantom test failure.

## Left for the next session

- **Root-fix the script-toolchain trap**: `scripts/*.sh` that exec go should self-select
  go_1_27 (or refuse with a pointer to `nix develop -c`) instead of dying on the floor.
- **First future tag exercises the FIXED release-draft workflow** — watch it draft cleanly,
  then publish via `gh release edit <tag> --draft=false --latest`.
- **scripts/verify-release.sh check 5**: consider excluding the Release-draft workflow from
  the green-commit scan (meta-tooling, not a quality gate) or the known-exception list grows
  with every release cut before `83962f4`.
- pkg.go.dev fetch 404'd at session end (indexing lag); confirm the v0.10.1 page renders.
- TODO_LIST row "Toolchain bump go1.26.7 → 1.26.8" is stale (floor is 1.27.1 now) — triage
  on the next blocked-row sweep.
