# TODO List

> Short-term, actionable, bounded work items, verified against the actual
> code (docs-health HARVEST passes 2026-09-03 through 2026-09-18 — closed
> items live in `CHANGELOG.md`, never here). For long-term vision and
> unrefined ideas, see ROADMAP.md.

## Status legend

| Status           | Meaning                                                 |
| ---------------- | ------------------------------------------------------- |
| 🔴 `TODO`        | Not started. Needs doing.                               |
| 🟡 `IN_PROGRESS` | Actively being worked on.                               |
| 🔵 `BLOCKED`     | Cannot proceed; external dependency or decision needed. |

## Next Up

Harvested 2026-09-18 (docs-health pass over the 2026-09-17/18 reports).
Ground truth: `Version = "0.9.0"`, go-health v0.2.0, templ-components
v1.18.0, 281 top-level test/benchmark/fuzz functions across 39 test files
(`rg -c '^func (Test\|Benchmark\|Fuzz)' *_test.go`).

| Task                                                                  | Status    | Why it matters / notes                                                                                                                                                                        | Evidence                                                            |
| --------------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Boot the full compose stack e2e (live scrape, all 8 panels, screenshot) | 🔴 `TODO` | Grafana provisioning is verified standalone; the panels have never executed against a live scrape. Eyeball the dashboard, capture a PNG for the deploy docs.                                  | `deploy/docker-compose.yml`; 06-12 report §b1                        |
| Write `deploy/README.md`                                              | 🔴 `TODO` | Ports, provisioning layout, anonymous-viewer security posture, teardown, and the duration-gauge panel — the stack exists but is undocumented.                                                 | `deploy/` (no README); 06-12 report §c, §f7/f35                      |
| Add `scripts/set-tag-protection.sh` (rulesets-as-code)                | 🔴 `TODO` | The `protect-release-tags` ruleset (id 23637848) exists only in GitHub; an idempotent PUT script makes it reproducible and fleet-replicable (encodes the three 422 API quirks).                | 06-12 report §c, §f8; `docs/release-checklist.md` §1                 |
| verify-dep-bump.sh: read the coverage floor from ci.yml               | 🔴 `TODO` | The 80% floor is hardcoded in both the script and ci.yml — a drift-ready split brain. Script should grep the floor out of ci.yml so CI stays the single source.                                | `scripts/verify-dep-bump.sh:41`; 06-12 report §f3/e3                 |
| verify-dep-bump.sh: daemon-proof the fmt check                        | 🔴 `TODO` | The dirty-tree check false-failed once when the auto-daemon committed the fmt output mid-run. Retry-once before trusting a failure; the race is documented in AGENTS.md.                       | 06-12 report §d4/e3; AGENTS.md "Gates race the auto-daemon" gotcha   |
| verify-dep-bump.sh: devShell-independent `go tool cover`              | 🔴 `TODO` | The script says "run OUTSIDE the devShell" but then needs `go` on PATH for the coverage parse — run the parse via `nix develop -c` or state the requirement honestly.                          | 06-12 report §b2d/e10                                                |
| CHANGELOG per-bullet tag audit v0.3.0–v0.8.0                          | 🔴 `TODO` | `[0.1.0]`/`[0.2.0]` are tag-verified; the remaining sections are spot-checked only, not diffed feature-by-feature against their tags.                                                          | 05-41 report §b5; CHANGELOG audit notes                              |
| Pin a `shutting_down:true` webhook payload case                       | 🔴 `TODO` | The v0.2.0 metadata pass-through is pinned; the true branch of the shutting-down flag is unpinned at wire level.                                                                               | `webhook_payload_test.go`; 05-41 report §f9                          |
| Check `testdata/golden/source.html` against Since-carrying tooltips   | 🔴 `TODO` | Source-mode golden predates the tooltip pairing; verify/regenerate so grouped cards pin the current render.                                                                                    | `testdata/golden/source.html`; 05-41 report §f10; 20-44 report §f46  |
| M88: keyboard-navigation a11y smoke                                   | 🔴 `TODO` | Tab order + visible focus on the newer controls (filter, pill, collapse) — the axe audit is structural, not keyboard-based.                                                                    | `browser_test.go`; 05-41 report §f5                                  |
| M89: metrics endpoint under strict CSP (browser test)                 | 🔴 `TODO` | `/health/metrics` is served under `WithMiddleware`/CSP in prod; the browser suite never requests it under the strict policy.                                                                   | 05-41 report §f6                                                     |
| M91: browser-suite startup latency measurement                        | 🔴 `TODO` | Serialized Chrome launches vs the 45s announce timeout — measure and record in the research doc; maybe the serialization can shrink again.                                                      | 05-41 report §f7; `docs/research/2026-09-10_benchmarks.md`           |
| M92: load-test env knobs beyond the 20×3 fixture                      | 🔴 `TODO` | Parameterize `loadtest_test.go` (sources/clients/cadence) and re-run with evidence enabled.                                                                                                    | 05-41 report §f8; `loadtest_test.go`                                 |
| M103: unit-test the version-guard grep logic                          | 🔴 `TODO` | The version-guard job greps the const/tag; a test pins the script logic against drift.                                                                                                         | 05-41 report §f12; `.github/workflows/ci.yml`                        |
| M105: doc.go combo examples (webhook+public-mode, `WithBasePath`)     | 🔴 `TODO` | The two most-asked combinations have no package-doc example.                                                                                                                                   | 05-41 report §f13                                                    |
| M106: amend the bisect-wall audit with the 2026-09-17 scars           | 🔴 `TODO` | Three more sessions, two trees of daemon carriers since the audit; the skip-list should cover them.                                                                                            | `docs/status/archived/2026-09-04_19-15_bisectability-audit.md`       |
| M107: nightly job verifying a Release page exists for every tag       | 🔴 `TODO` | Catches a missed `gh release create` mechanically instead of by memory.                                                                                                                        | 05-41 report §f11                                                    |
| Mobile visual check of the metadata line in the stacked layout        | 🔴 `TODO` | Human-eye pass on a real viewport screenshot; axe covers structure, not readability.                                                                                                           | 20-44 report §f23; 05-41 report §f16                                 |
| Dedicated dark-mode contrast measurement                              | 🔴 `TODO` | WCAG-AA is locked for status colors since v0.8.0; the dim metadata line has no measured ratio.                                                                                                 | 20-44 report §f24; 05-41 report §b8/f17                              |
| Re-run the aggregate load test on the v0.9.0 render                   | 🔴 `TODO` | The 2026-09-10 CPU numbers predate the metadata line; refresh them.                                                                                                                            | `docs/research/2026-09-10_aggregate-load-test.md`; 20-44 report §f45 |
| Blocked-row premise triage                                            | 🔴 `TODO` | Re-verify every 🔵 row's premise against the current tree — point-in-time reports rot.                                                                                                         | 20-44 report §f30; 05-41 report §f19                                 |
| Exercise `release-draft.yml` on the next tag push                     | 🔴 `TODO` | The workflow is lint-clean but has never run against a real tag; publish the draft per checklist §6.                                                                                           | 05-41 report §b7; `docs/release-checklist.md`                        |
| Inspect a CI browser run for the screenshot artifacts                 | 🔴 `TODO` | The M78 artifact step is written and actionlint-clean but never inspected by eye in a real run.                                                                                                | 05-41 report §b6/f3                                                  |
| Toolchain bump go1.26.7 → 1.26.8 via verify-dep-bump.sh               | 🔴 `TODO` | The CI matrix already tests 1.26.8; align the go.mod floor through the standard gate chain.                                                                                                    | 05-41 report §f40; `.github/workflows/ci.yml` matrix                 |
| Sweep ROADMAP v1.0 criteria against current state                     | 🔴 `TODO` | Annotate which of the four criteria are met (golden-locked contracts largely are; API freeze/consumer signal are not).                                                                          | `ROADMAP.md` "v1.0 Criteria"; 06-12 report §f24                      |
| pre-push-checks.sh: fuzz.yml-vs-targets consistency check             | 🔴 `TODO` | `grep 'func Fuzz' fuzz_test.go` vs the workflow steps — the registry rule, mechanized at the desk.                                                                                             | 06-12 report §f28; `scripts/pre-push-checks.sh`                      |
| Digest-pin prometheus/grafana images + compose dependabot             | 🔴 `TODO` | Demo-stack supply chain: image bumps should be proposed, not silent.                                                                                                                           | `deploy/docker-compose.yml`; 06-12 report §e9/f16                    |
| Compose healthchecks (prometheus ready before grafana)                | 🔴 `TODO` | `depends_on` orders startup, not readiness; Grafana can boot before the datasource exists.                                                                                                     | `deploy/docker-compose.yml`; 06-12 report §f17                       |
| shellcheck for `scripts/*.sh` in CI                                   | 🔴 `TODO` | Five gate scripts with zero static shell analysis.                                                                                                                                             | 06-12 report §f18                                                    |
| Write `docs/metrics.md`                                               | 🔴 `TODO` | Every exported metric, labels, cardinality guarantees — one consumer-facing reference instead of README + code spelunking.                                                                     | 06-12 report §f19; `metrics.go`                                      |
| Grafana threshold-alert example (`dashboard_health_up == 0`)          | 🔴 `TODO` | Provisioned alerting as a commented example in the demo dashboard.                                                                                                                             | `deploy/grafana/`; 06-12 report §f20                                 |
| Upstream filings (templ formatting, erraudit ×2)                      | 🔴 `TODO` | templ's gofumpt-incompatible raw output; erraudit `strings.Cut` FP; erraudit `nolint-audit ./...` silent no-op. verify-before-filing gates each.                                                | 18-13 report §f4/f5; 14-55 report §c                              |

Everything else from the v0.3.x–v0.9.0 cycles either shipped (see
`CHANGELOG.md`), was closed with a reason in the annotated reports under
`docs/status/` (fully-executed reports move to `archived/`), or lives in
ROADMAP.md as raw ideas. v0.9.0 (2026-09-17, commit `5f71fa6`) is Latest.
Session-closing gate: `scripts/pre-push-checks.sh`; dependency bumps have
`scripts/verify-dep-bump.sh`; the templ-components v1.18.0 sweep
(2026-09-18) was adopted with the pin-guard update and a green browser
suite. Known-broken-commit SHAs for `git bisect skip`: see AGENTS.md and
`docs/status/archived/2026-09-04_19-15_bisectability-audit.md`.

## Blocked (needs user decision)

Batching note: most rows below are product decisions that could be
answered in ONE user session (06-12 report §f32) — tag-ruleset bypass,
PVR, release timing, deploy-CI policy, CV rollout, copy affordance,
build-tag gating, fingerprint stability, evidence posture, pin-guard
sign-off.

| Task                                                    | Status       | Why blocked                                                                                                                                                                                                                                                                                    | Evidence                                                                                               |
| ------------------------------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| go-health v0.3.0 consumer batch (federation)            | 🔵 `BLOCKED` | go-health's `health/federation` is implemented and verified but UNRELEASED (v0.3.0 vehicle, blocked on tag/push authorization + scope coordination between two sessions). Once tagged: bump go.mod, permanent federation→dashboard integration test + the `dashboard.Prober` compile-time assertion, a federation example (2 mock upstreams + hub) with a nix app, evidence-strip verification that `name/reachable` rows count as non-pass observations, and federation-fed seam tests (untrusted remote error strings through metric-label escaping, webhooks, HTML). | 09-47 report §b1/§c2-c6/§e10; `ROADMAP.md` §2; go-health `docs/federation-design.md` |
| CV-side adoption: bump go.mod, deploy, verify live page | 🔵 `BLOCKED` | Deploy pipeline + rollout order are the user's call (report g Q2). Verified 2026-09-10 (read-only): CV serves a CSP-safe mini-client, not the Datastar SDK — a safe bump needs `dashboard.WithNoDatastarRuntime()` (shipped in v0.7.0) or the filter/pill would render dead                        | CV `internal/di/health_dashboard.go` pins v0.6.1                                                         |
| Copy affordance for raw check keys                      | 🔵 `BLOCKED` | Product decision (report g Q3): title-attr + select-text vs a Datastar clipboard action                                                                                                                                                                                                 | status report 2026-09-09 question 3                                                                      |
| Pin-guard keep sign-off                                 | 🔵 `BLOCKED` | Guard maintained through the seventh sweep (v1.18.0, 2026-09-18: pins updated in the adoption change after the browser suite ran green); the deviation from the original removal condition still wants formal sign-off                                                                              | `scripts/check-ui-pins.sh` header                                                                        |
| Build-tag gating for SSE                                | 🔵 `BLOCKED` | Consumers who only want HTML shouldn't need GOEXPERIMENT=jsonv2. Requires decision: accept, fork go-sse, or gate.                                                                                                                                                                         | `ROADMAP.md` Open Questions                                                                              |
| Fingerprint format stability                            | 🔵 `BLOCKED` | Length-prefix fix changed fingerprint values; documented as accepted in CHANGELOG pending a versioning decision.                                                                                                                                                                          | `ROADMAP.md` Open Questions                                                                              |
| Evidence strip machine contract + persistence           | 🔵 `BLOCKED` | Two product decisions from the evidence-strip design (report g Q2/Q3): should proven/unproven counts surface in metrics + trend/export JSON (HTML-only contract today), and should the observation window persist across restarts (opt-in store) instead of resetting?                            | `docs/status/archived/2026-09-10_03-24_evidence-strip-health-washing-status.md` §g; `ROADMAP.md` Open Questions   |
| Compose-stack CI policy                                 | 🔵 `BLOCKED` | Manual-only demo (documented), build-only CI job, or full boot+probe CI? Costs CI minutes and Docker-in-CI surface; gates the deploy-e2e and M107-class jobs.                                                                                                                             | 06-12 report §g1; `ROADMAP.md` Open Questions                                                            |
| Tag-ruleset bypass policy                               | 🔵 `BLOCKED` | `protect-release-tags` is live with `bypass: never` (strongest: break-glass = disabling the rule in the web UI). Keep `never`, or add repository-admin as a bypass actor for emergency re-tags?                                                                                            | 06-12 report §g2; `docs/release-checklist.md` §1                                                         |
| Enable private vulnerability reporting                  | 🔵 `BLOCKED` | SECURITY.md references PVR, which is disabled on the repo; enabling is a repo-admin action (or delegated via `gh api` with authorization).                                                                                                                                                  | 05-41 report §f33; `SECURITY.md`                                                                         |
| v0.10.0 release timing                                  | 🔵 `BLOCKED` | `[Unreleased]` carries the export wire growth, evidence tooltips, the Grafana stack, the bump gate, and the v1.18.0 adoption. Trigger (calendar / batch size / "when CV needs it") or cut now via the tag-first checklist?                                                                  | 06-12 report §g3; `CHANGELOG.md` `[Unreleased]`                                                          |
| Unskip go-structure-linter in BuildFlow                 | 🔵 `BLOCKED` | BuildFlow pins go-structure-linter v0.10.0, whose SDK predates project-config support — the committed `.go-structure-linter.yaml` (`flat` preset for this deliberately flat root-package library) is inert, so the step is skipped in `.buildflow.yml`. Unskip when BuildFlow pins a release where `Lint` calls `LoadProjectConfig`. | go-structure-linter v0.10.0 `pkg/sdk/sdk.go` (no `LoadProjectConfig`) vs current checkout `sdk.go:132`   |
| Unskip branching-flow in BuildFlow                      | 🔵 `BLOCKED` | The step gates on 24 PHANTOM_TYPE findings spanning the public option API (breaking redesign → versioning decision) and 2 BOOL_BLIND bit-flag demands; no scoping exists (`//nolint` ignored by phantom/boolblind, `.branching-flow.yml` has no rule exclusion, BuildFlow's provider hardcodes `analysis.RunAll`). Unskip after a fleet decision. | `.buildflow.yml` skip rationale; branching-flow `pkg/core/ignore_comments.go` consumers (roleak/do only) |
| Unskip templ-generate in BuildFlow (fleet DAG ordering) | 🔵 `BLOCKED` | BuildFlow lets nix-evaluating steps run concurrently with tree-mutating generators; a nix `git+file` source snapshot ingested during templ's raw-output window made treefmt-check fail on a tree state that no longer existed (verified 2026-09-16). Unskip when BuildFlow orders all generators before all nix-evaluating steps; also decide upstream-file vs local-patch. | `/tmp/bf-final.log` timeline; `.buildflow.yml` skip rationale; status report 2026-09-16 §c               |
